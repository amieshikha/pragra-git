# Recap: Unix ? Kernel, Shell
touch, mkdir, ls

ls ls -l

/

~

// grep , sort

Grep
Sort
Permissions
OWNER GROUP PUBLIC create a file -- change permission

==Shell script ==

created a file .sh extension
myfile.sh

execute the file ------- > ./myfile.sh
changed the permission to exectue chmod +x myfile.sh ----> execution
added echo "Hello World"!

example

create a file and print your name after running the file.
#!/bin/bash

echo "Enter a number" read number

if [ $number -gt 0 ]; then echo "$number is positive" elif [$number -lt 0]; then echo "$number is positive" else echo "$number is zero" fi

else if ---> elif

